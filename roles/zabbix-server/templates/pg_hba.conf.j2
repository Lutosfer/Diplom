# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Разрешаем подключения от сервера Zabbix
host    {{ db_name }}   {{ db_user }}		{{ zabbix_server_ip }}/32    md5
local	{{ db_name }}   {{ db_user }}				trust
# Локальные подключения
local   all             all					peer
host    all             all             127.0.0.1/32		md5
host    all             all		{{ zabbix_server_ip }}/32		md5

